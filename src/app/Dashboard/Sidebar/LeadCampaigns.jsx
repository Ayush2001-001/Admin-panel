'use client';
import { Box } from "@mui/material";
import Link from "next/link";
export default function LeadCompaigns () {
    return(
        <Box>
        <Link href={"/LeadCompaigns"}>LeadCompaigns</Link>
       </Box>
    );
 }